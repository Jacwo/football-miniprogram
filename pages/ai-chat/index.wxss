/* pages/ai-chat/index.wxss */

/* ========== 全局 ========== */
.chat-page {
  display: flex;
  flex-direction: column;
  height: 100vh;
  background: #f0f2f5;
  overflow: hidden;
  box-sizing: border-box;
  transition: padding-bottom 0.25s ease;
}

/* ========== 左侧栏 ========== */
.sidebar-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(15, 23, 42, 0.5);
  z-index: 500;
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.28s cubic-bezier(0.4, 0, 0.2, 1);
}

.sidebar-mask.visible {
  opacity: 1;
  pointer-events: auto;
}

.sidebar-panel {
  position: fixed;
  top: 0;
  left: 0;
  bottom: 0;
  width: 580rpx;
  max-width: 85vw;
  background: #ffffff;
  z-index: 501;
  display: flex;
  flex-direction: column;
  transform: translateX(-100%);
  transition: transform 0.32s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: 4rpx 0 40rpx rgba(0, 0, 0, 0.08);
  border-radius: 0 28rpx 28rpx 0;
}

.sidebar-panel.open {
  transform: translateX(0);
}

.sidebar-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 40rpx 32rpx 24rpx;
  border-bottom: 1rpx solid #f1f5f9;
  flex-shrink: 0;
}

.sidebar-title {
  font-size: 34rpx;
  font-weight: 700;
  color: #0f172a;
  letter-spacing: 0.5rpx;
}

.sidebar-close {
  width: 56rpx;
  height: 56rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  background: #f1f5f9;
  transition: all 0.2s;
}

.sidebar-close:active {
  background: #e2e8f0;
  transform: scale(0.93);
}

.sidebar-close-icon {
  font-size: 36rpx;
  color: #64748b;
  line-height: 1;
  font-weight: 300;
}

.sidebar-loading {
  display: flex;
  justify-content: center;
  padding: 80rpx 0;
  color: #94a3b8;
  font-size: 26rpx;
}

.sidebar-list {
  flex: 1;
  min-height: 0;
  padding: 16rpx 0;
}

.sidebar-empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 120rpx 48rpx;
  gap: 16rpx;
}

.sidebar-empty-text {
  font-size: 28rpx;
  color: #94a3b8;
  font-weight: 500;
}

.sidebar-empty-desc {
  font-size: 24rpx;
  color: #cbd5e1;
}

/* 分组标签 */
.session-group-label {
  font-size: 22rpx;
  color: #94a3b8;
  font-weight: 600;
  padding: 24rpx 32rpx 10rpx;
  text-transform: uppercase;
  letter-spacing: 1rpx;
}

/* 会话列表项 */
.session-item {
  position: relative;
  display: flex;
  flex-direction: column;
  gap: 6rpx;
  padding: 22rpx 32rpx;
  margin: 4rpx 16rpx;
  border-radius: 16rpx;
  transition: all 0.2s ease;
  border: 2rpx solid transparent;
}

.session-item:active {
  background: #f8fafc;
  transform: scale(0.98);
}

.session-item.active {
  background: linear-gradient(135deg, #eff6ff 0%, #f0f9ff 100%);
  border-color: #bae6fd;
}

/* 主行：比赛信息左 + 时间右 */
.session-row {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  gap: 16rpx;
}

.session-info {
  flex: 1;
  min-width: 0;
  display: flex;
  align-items: center;
  gap: 10rpx;
}

.session-league-tag {
  flex-shrink: 0;
  font-size: 20rpx;
  font-weight: 600;
  color: #fff;
  background: #475569;
  padding: 4rpx 12rpx;
  border-radius: 6rpx;
  line-height: 1.5;
}

.session-name {
  font-size: 28rpx;
  font-weight: 600;
  color: #1e293b;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.session-time {
  font-size: 22rpx;
  color: #94a3b8;
  flex-shrink: 0;
}

.session-agent {
  font-size: 22rpx;
  color: #3b82f6;
  padding-left: 0;
}

.session-check {
  position: absolute;
  right: 32rpx;
  top: 50%;
  transform: translateY(-50%);
  width: 44rpx;
  height: 44rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, #2563eb 0%, #6366f1 100%);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 24rpx;
  color: #fff;
  font-weight: 600;
  box-shadow: 0 2rpx 12rpx rgba(37, 99, 235, 0.35);
}

/* ========== 悬浮可拖动按钮 ========== */
.floating-btn-area {
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  pointer-events: none;
  z-index: 450;
}

.floating-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 88rpx;
  height: 88rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, #0d9488 0%, #14b8a6 100%);
  box-shadow: 0 8rpx 28rpx rgba(13, 148, 136, 0.35);
  pointer-events: auto;
  transition: transform 0.15s ease, box-shadow 0.2s;
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  white-space: nowrap;
}

.floating-btn:active {
  transform: scale(0.9);
  box-shadow: 0 4rpx 16rpx rgba(13, 148, 136, 0.25);
}

.floating-btn.active {
  background: linear-gradient(135deg, #0f766e 0%, #0d9488 100%);
  box-shadow: 0 8rpx 28rpx rgba(13, 148, 136, 0.5);
}

.chat-icon-emoji {
  font-size: 36rpx;
  line-height: 1;
  color: #fff;
}

/* ========== 消息列表 ========== */
.message-list {
  flex: 1;
  min-height: 0;
  padding: 20rpx 0;
}

/* 欢迎区域 */
.welcome-section {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 80rpx 48rpx 40rpx;
}

.welcome-icon {
  margin-bottom: 36rpx;
  width: 120rpx;
  height: 120rpx;
  background: #ffffff;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 12rpx 40rpx rgba(0, 0, 0, 0.08), 0 0 0 8rpx rgba(59, 130, 246, 0.05);
}

.ai-icon {
  width: 80rpx;
  height: 80rpx;
}

.welcome-title {
  font-size: 44rpx;
  font-weight: 700;
  color: #0f172a;
  margin-bottom: 16rpx;
  letter-spacing: 1rpx;
}

.welcome-desc {
  font-size: 28rpx;
  color: #64748b;
  text-align: center;
  line-height: 1.7;
}

/* ========== 比赛&智能体选择区 ========== */
.setup-section {
  padding: 0 28rpx 28rpx;
  display: flex;
  flex-direction: column;
  gap: 28rpx;
}

.setup-step {
  display: flex;
  flex-direction: column;
  gap: 18rpx;
}

.setup-step-header {
  display: flex;
  align-items: center;
  gap: 14rpx;
}

.setup-step-num {
  width: 44rpx;
  height: 44rpx;
  background: linear-gradient(135deg, #2563eb 0%, #6366f1 100%);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 24rpx;
  color: #fff;
  font-weight: 700;
  box-shadow: 0 4rpx 14rpx rgba(37, 99, 235, 0.3);
}

.setup-step-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #0f172a;
}

.setup-step-desc {
  font-size: 24rpx;
  color: #94a3b8;
  padding-left: 58rpx;
}

/* 已选比赛卡片 */
.setup-match-selected {
  padding: 28rpx;
  background: linear-gradient(135deg, #eff6ff 0%, #e0f2fe 100%);
  border-radius: 20rpx;
  border: 2rpx solid #bae6fd;
  display: flex;
  flex-direction: column;
  gap: 8rpx;
  box-shadow: 0 4rpx 16rpx rgba(59, 130, 246, 0.06);
}

.setup-match-text {
  font-size: 32rpx;
  font-weight: 700;
  color: #0f172a;
}

.setup-match-sub {
  font-size: 24rpx;
  color: #64748b;
}

.setup-match-switch {
  font-size: 22rpx;
  color: #2563eb;
  margin-top: 4rpx;
  font-weight: 500;
}

/* 选择比赛按钮 */
.setup-match-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 18rpx;
  padding: 44rpx;
  background: #ffffff;
  border: 2rpx dashed #93c5fd;
  border-radius: 20rpx;
  transition: all 0.2s ease;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.03);
}

.setup-match-btn:active {
  transform: scale(0.98);
  background: #f8fafc;
  border-color: #60a5fa;
}

.setup-match-btn-icon {
  font-size: 40rpx;
}

.setup-match-btn-text {
  font-size: 30rpx;
  color: #2563eb;
  font-weight: 600;
}

/* 已选智能体 */
.setup-agent-selected {
  display: flex;
  align-items: center;
  gap: 16rpx;
  padding: 22rpx 28rpx;
  background: linear-gradient(135deg, #f0fdf4 0%, #dcfce7 100%);
  border-radius: 18rpx;
  border: 2rpx solid #bbf7d0;
  box-shadow: 0 2rpx 12rpx rgba(22, 163, 74, 0.06);
}

.setup-agent-back {
  width: 56rpx;
  height: 56rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  border-radius: 50%;
  background: rgba(22, 163, 74, 0.1);
  transition: all 0.2s;
}

.setup-agent-back:active {
  background: rgba(22, 163, 74, 0.2);
  transform: scale(0.93);
}

.setup-agent-back-icon {
  font-size: 44rpx;
  color: #16a34a;
  font-weight: 300;
  line-height: 1;
}

.setup-agent-avatar {
  width: 76rpx;
  height: 76rpx;
  border-radius: 50%;
  flex-shrink: 0;
  background: #f1f5f9;
  border: 3rpx solid #e2e8f0;
}

.setup-agent-name {
  flex: 1;
  font-size: 30rpx;
  font-weight: 700;
  color: #166534;
  min-width: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.setup-agent-reselect {
  flex-shrink: 0;
  padding: 8rpx 16rpx;
}

.setup-agent-reselect:active {
  opacity: 0.6;
}

.setup-agent-switch-tip {
  font-size: 24rpx;
  color: #16a34a;
  font-weight: 600;
}

/* 智能体列表 */
.agent-grid {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.agent-card {
  display: flex;
  align-items: center;
  padding: 24rpx;
  background: #ffffff;
  border-radius: 20rpx;
  border: 2rpx solid #f1f5f9;
  transition: all 0.2s ease;
  box-shadow: 0 1rpx 4rpx rgba(0, 0, 0, 0.02);
}

.agent-card:active {
  transform: scale(0.98);
  border-color: #93c5fd;
  box-shadow: 0 4rpx 16rpx rgba(37, 99, 235, 0.12);
}

.agent-card.agent-default {
  border-color: #bfdbfe;
  background: linear-gradient(135deg, #eff6ff 0%, #ffffff 100%);
}

.agent-avatar {
  width: 88rpx;
  height: 88rpx;
  border-radius: 50%;
  flex-shrink: 0;
  background: #f1f5f9;
  border: 3rpx solid #e2e8f0;
}

.agent-info {
  flex: 1;
  margin-left: 22rpx;
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: 6rpx;
}

.agent-name-row {
  display: flex;
  align-items: center;
  gap: 10rpx;
}

.agent-name {
  font-size: 30rpx;
  font-weight: 700;
  color: #1e293b;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.agent-default-tag {
  padding: 4rpx 14rpx;
  background: linear-gradient(135deg, #dbeafe 0%, #bae6fd 100%);
  border-radius: 8rpx;
  flex-shrink: 0;
  font-size: 20rpx;
  color: #0369a1;
  font-weight: 600;
}

.agent-desc {
  font-size: 24rpx;
  color: #64748b;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.agent-factor {
  font-size: 22rpx;
  color: #94a3b8;
}

.agent-arrow {
  font-size: 36rpx;
  color: #cbd5e1;
  margin-left: 16rpx;
  flex-shrink: 0;
  font-weight: 300;
}

.agent-loading {
  display: flex;
  justify-content: center;
  padding: 40rpx 0;
  color: #94a3b8;
  font-size: 26rpx;
}

.agent-empty {
  display: flex;
  justify-content: center;
  padding: 60rpx 0;
}

.agent-empty-text {
  font-size: 26rpx;
  color: #94a3b8;
}

/* 新建智能体按钮 */
.create-agent-btn {
  margin-top: 16rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 14rpx;
  padding: 30rpx;
  border: 2rpx dashed #93c5fd;
  border-radius: 20rpx;
  background: #ffffff;
  transition: all 0.2s ease;
  box-shadow: 0 1rpx 4rpx rgba(0, 0, 0, 0.02);
}

.create-agent-btn:active {
  transform: scale(0.98);
  background: #f8fafc;
  border-color: #60a5fa;
}

.create-agent-icon {
  font-size: 34rpx;
  color: #2563eb;
  font-weight: 300;
  line-height: 1;
}

.create-agent-text {
  font-size: 28rpx;
  color: #2563eb;
  font-weight: 600;
}

/* ========== 消息项 ========== */
.message-item {
  margin-bottom: 12rpx;
  animation: fadeInUp 0.3s ease;
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(24rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.list-footer {
  height: 40rpx;
}

/* ========== 底部控制区 ========== */
.bottom-section {
  flex-shrink: 0;
  background: #ffffff;
  box-shadow: 0 -2rpx 16rpx rgba(0, 0, 0, 0.04);
  padding-bottom: env(safe-area-inset-bottom);
  border-radius: 28rpx 28rpx 0 0;
}

/* 紧凑信息栏 */
.info-bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 16rpx 24rpx 8rpx;
  gap: 12rpx;
}

.info-bar-left {
  display: flex;
  align-items: center;
  gap: 8rpx;
  padding: 10rpx 20rpx;
  background: #f1f5f9;
  border-radius: 24rpx;
  min-width: 0;
  flex-shrink: 1;
  transition: all 0.2s;
}

.info-bar-left:active {
  background: #e2e8f0;
}

.info-bar-match {
  font-size: 24rpx;
  color: #1e293b;
  font-weight: 600;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.info-bar-match-empty {
  font-size: 24rpx;
  color: #94a3b8;
  white-space: nowrap;
}

.info-bar-arrow {
  font-size: 18rpx;
  color: #94a3b8;
  margin-left: 2rpx;
  flex-shrink: 0;
}

.info-bar-right {
  display: flex;
  align-items: center;
  gap: 12rpx;
  flex-shrink: 0;
}

.info-bar-agent {
  display: flex;
  align-items: center;
  gap: 6rpx;
  padding: 8rpx 16rpx;
  background: #f0fdf4;
  border-radius: 24rpx;
  white-space: nowrap;
  transition: all 0.2s;
}

.info-bar-agent:active {
  background: #dcfce7;
}

.info-bar-agent-text {
  font-size: 22rpx;
  color: #16a34a;
  font-weight: 600;
  max-width: 160rpx;
  overflow: hidden;
  text-overflow: ellipsis;
}

.info-bar-agent-switch {
  font-size: 28rpx;
  color: #16a34a;
  font-weight: 400;
  line-height: 1;
}

.info-bar-model {
  padding: 8rpx 14rpx;
  background: #eff6ff;
  border-radius: 24rpx;
  border: 1rpx solid #bfdbfe;
}

.info-bar-model-text {
  font-size: 20rpx;
  color: #2563eb;
  font-weight: 600;
}

.info-bar-clear {
  padding: 10rpx 22rpx;
  background: #fef2f2;
  border-radius: 24rpx;
  transition: all 0.2s;
}

.info-bar-clear:active {
  background: #fee2e2;
}

.info-bar-clear text {
  font-size: 22rpx;
  color: #ef4444;
  font-weight: 600;
}

/* 正在生成提示 */
.generating-tip {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 14rpx 24rpx;
  background: linear-gradient(135deg, #fef3c7 0%, #fef9c3 100%);
}

.tip-text {
  font-size: 26rpx;
  color: #b45309;
  font-weight: 600;
  display: flex;
  align-items: center;
}

.tip-text::before {
  content: '';
  display: inline-block;
  width: 16rpx;
  height: 16rpx;
  background-color: #f59e0b;
  border-radius: 50%;
  margin-right: 12rpx;
  animation: pulse 1.5s infinite;
}

@keyframes pulse {
  0%, 100% {
    opacity: 1;
    transform: scale(1);
  }
  50% {
    opacity: 0.4;
    transform: scale(0.75);
  }
}

.stop-btn {
  padding: 12rpx 32rpx;
  background: linear-gradient(135deg, #ef4444 0%, #f87171 100%);
  border-radius: 24rpx;
  box-shadow: 0 4rpx 14rpx rgba(239, 68, 68, 0.3);
  transition: all 0.2s;
}

.stop-btn:active {
  transform: scale(0.95);
}

.stop-text {
  font-size: 24rpx;
  color: #ffffff;
  font-weight: 600;
}

/* 输入框容器 */
.input-wrapper {
  display: flex;
  align-items: center;
  gap: 12rpx;
  background: #f1f5f9;
  border-radius: 32rpx;
  padding: 14rpx 14rpx 14rpx 32rpx;
  margin: 14rpx 20rpx 20rpx;
  border: 2rpx solid transparent;
  transition: all 0.25s ease;
}

.input-wrapper:focus-within {
  border-color: #2563eb;
  background: #ffffff;
  box-shadow: 0 0 0 6rpx rgba(37, 99, 235, 0.08);
}

.chat-input {
  flex: 1;
  min-height: 44rpx;
  max-height: 120rpx;
  font-size: 28rpx;
  color: #1e293b;
  line-height: 1.5;
}

.input-placeholder {
  color: #94a3b8;
}

.send-btn {
  width: 68rpx;
  height: 68rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #e2e8f0;
  border-radius: 50%;
  flex-shrink: 0;
  transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
}

.send-btn.active {
  background: linear-gradient(135deg, #2563eb 0%, #6366f1 100%);
  box-shadow: 0 6rpx 22rpx rgba(37, 99, 235, 0.4);
}

.send-btn.active:active {
  transform: scale(0.9);
}

.send-icon {
  font-size: 32rpx;
  color: #94a3b8;
  font-weight: 700;
  transition: color 0.25s ease;
}

.send-btn.active .send-icon {
  color: #ffffff;
}

/* ========== 智能体详情弹窗 ========== */
.agent-detail-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(15, 23, 42, 0.5);
  z-index: 1000;
  display: flex;
  align-items: flex-end;
  animation: maskIn 0.25s ease;
}

@keyframes maskIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

.agent-detail-popup {
  width: 100%;
  max-height: 82vh;
  background: #ffffff;
  border-radius: 36rpx 36rpx 0 0;
  display: flex;
  flex-direction: column;
  animation: popupIn 0.32s cubic-bezier(0.32, 0.72, 0, 1);
}

@keyframes popupIn {
  from { transform: translateY(100%); }
  to { transform: translateY(0); }
}

.detail-header {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 36rpx 32rpx;
  border-bottom: 1rpx solid #f1f5f9;
  position: relative;
}

.detail-close {
  position: absolute;
  left: 32rpx;
  width: 52rpx;
  height: 52rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  background: #f1f5f9;
  transition: all 0.2s;
}

.detail-close:active {
  background: #e2e8f0;
  transform: scale(0.92);
}

.close-icon {
  font-size: 36rpx;
  color: #64748b;
  line-height: 1;
  font-weight: 300;
}

.detail-title {
  font-size: 34rpx;
  font-weight: 700;
  color: #0f172a;
}

/* 详情头部操作按钮 */
.detail-actions {
  position: absolute;
  right: 32rpx;
  top: 50%;
  transform: translateY(-50%);
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.detail-action-btn {
  padding: 10rpx 22rpx;
  border-radius: 24rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 24rpx;
  font-weight: 600;
  transition: all 0.2s ease;
}

.detail-action-btn.edit {
  background: #eff6ff;
  color: #2563eb;
  border: 1rpx solid #bfdbfe;
}

.detail-action-btn.edit:active {
  background: #dbeafe;
}

.detail-action-btn.delete {
  background: #fef2f2;
  color: #ef4444;
  border: 1rpx solid #fecaca;
}

.detail-action-btn.delete:active {
  background: #fee2e2;
}

.detail-loading {
  display: flex;
  justify-content: center;
  padding: 80rpx 0;
  color: #94a3b8;
  font-size: 28rpx;
}

.detail-scroll {
  flex: 1;
  min-height: 0;
  max-height: 72vh;
  box-sizing: border-box;
}

.detail-content {
  width: 100%;
  box-sizing: border-box;
  padding: 28rpx 32rpx;
  padding-bottom: calc(56rpx + env(safe-area-inset-bottom));
  overflow-x: hidden;
}

/* 智能体基本信息块 */
.detail-agent-info {
  display: flex;
  align-items: center;
  padding: 28rpx;
  background: linear-gradient(135deg, #f8fafc 0%, #eff6ff 100%);
  border-radius: 20rpx;
  margin-bottom: 24rpx;
}

.detail-avatar {
  width: 108rpx;
  height: 108rpx;
  border-radius: 50%;
  flex-shrink: 0;
  background: #f1f5f9;
  border: 4rpx solid #e2e8f0;
}

.detail-agent-meta {
  flex: 1;
  margin-left: 24rpx;
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.detail-name-row {
  display: flex;
  align-items: center;
  gap: 12rpx;
  flex-wrap: wrap;
}

.detail-agent-name {
  font-size: 34rpx;
  font-weight: 700;
  color: #0f172a;
}

.detail-tag {
  padding: 6rpx 18rpx;
  border-radius: 8rpx;
  font-size: 22rpx;
  font-weight: 600;
  flex-shrink: 0;
}

.detail-tag.default {
  background: linear-gradient(135deg, #dbeafe 0%, #bae6fd 100%);
  color: #0369a1;
}

.detail-tag.system {
  background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%);
  color: #b45309;
}

.detail-agent-desc {
  font-size: 26rpx;
  color: #64748b;
  line-height: 1.5;
  word-break: break-all;
  overflow-wrap: break-word;
}

.detail-factor-count {
  font-size: 24rpx;
  color: #2563eb;
  font-weight: 600;
}

/* 确认选择按钮 */
.detail-confirm-btn {
  width: 100%;
  height: 84rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, #10b981 0%, #059669 100%);
  border-radius: 42rpx;
  color: #ffffff;
  font-size: 30rpx;
  font-weight: 700;
  box-shadow: 0 8rpx 22rpx rgba(16, 185, 129, 0.25);
  margin-bottom: 28rpx;
  transition: all 0.2s ease;
}

.detail-confirm-btn:active {
  transform: scale(0.97);
  opacity: 0.9;
}

/* 因素配置区域 */
.detail-factor-section {
  display: flex;
  flex-direction: column;
  gap: 24rpx;
}

.factor-group {
  display: flex;
  flex-direction: column;
}

.factor-group-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 14rpx 8rpx;
  margin-bottom: 10rpx;
  border-bottom: 2rpx solid #f1f5f9;
}

.factor-group-name {
  font-size: 28rpx;
  font-weight: 700;
  color: #334155;
}

.factor-group-count {
  font-size: 20rpx;
  color: #94a3b8;
  background: #f1f5f9;
  padding: 2rpx 5rpx;
  border-radius: 16rpx;
}

.factor-list {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

.factor-item {
  padding: 20rpx 22rpx;
  background: #ffffff;
  border-radius: 16rpx;
  border: 1rpx solid #f1f5f9;
  display: flex;
  flex-direction: column;
  gap: 8rpx;
  transition: all 0.15s ease;
}

.factor-item.disabled {
  opacity: 0.5;
  background: #fafafa;
}

.factor-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
}

.factor-right-actions {
  display: flex;
  align-items: center;
  gap: 8rpx;
  flex-shrink: 0;
}

.factor-actions {
  display: flex;
  align-items: center;
  gap: 8rpx;
}

.factor-action-btn {
  width: 52rpx;
  height: 52rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.2s ease;
}

.factor-action-btn.edit {
  background: #eff6ff;
  border: 1rpx solid #bfdbfe;
}

.factor-action-btn.edit:active {
  background: #dbeafe;
  transform: scale(0.93);
}

.factor-action-btn.delete {
  background: #fef2f2;
  border: 1rpx solid #fecaca;
}

.factor-action-btn.delete:active {
  background: #fee2e2;
  transform: scale(0.93);
}

.factor-action-icon {
  font-size: 24rpx;
  color: #64748b;
  line-height: 1;
}

.factor-action-btn.edit .factor-action-icon {
  color: #2563eb;
}

.factor-action-btn.delete .factor-action-icon {
  color: #ef4444;
}

.factor-name-row {
  display: flex;
  align-items: center;
  gap: 10rpx;
  flex: 1;
  min-width: 0;
}

.factor-switch {
  width: 72rpx;
  height: 42rpx;
  border-radius: 21rpx;
  flex-shrink: 0;
}

.factor-switch .switch-thumb {
  width: 34rpx;
  height: 34rpx;
  top: 4rpx;
  left: 4rpx;
}

.factor-switch.active .switch-thumb {
  left: 34rpx;
}

.factor-switch.saving {
  opacity: 0.5;
  pointer-events: none;
}

.factor-switch-basic.active {
  background: #94a3b8;
  pointer-events: none;
  opacity: 0.55;
}

.factor-name {
  font-size: 28rpx;
  font-weight: 600;
  color: #1e293b;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.factor-code {
  font-size: 20rpx;
  color: #94a3b8;
  background: #f1f5f9;
  padding: 2rpx 8rpx;
  border-radius: 4rpx;
  font-family: monospace;
  flex-shrink: 0;
}

.factor-status {
  padding: 4rpx 14rpx;
  background: #fef2f2;
  border-radius: 8rpx;
  flex-shrink: 0;
}

.factor-status text {
  font-size: 20rpx;
  color: #ef4444;
  font-weight: 600;
}

.factor-desc {
  font-size: 24rpx;
  color: #64748b;
  line-height: 1.5;
  word-break: break-all;
  overflow-wrap: break-word;
}

.factor-prompt {
  margin-top: 8rpx;
  padding: 14rpx 18rpx;
  background: #fffbf0;
  border-radius: 12rpx;
  border-left: 4rpx solid #f59e0b;
}

.factor-prompt-label {
  display: block;
  font-size: 20rpx;
  color: #b45309;
  font-weight: 600;
  margin-bottom: 6rpx;
}

.factor-prompt-text {
  display: -webkit-box;
  font-size: 22rpx;
  color: #78350f;
  line-height: 1.6;
  word-break: break-all;
  overflow-wrap: break-word;
  max-height: 80rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
}

.detail-empty-factor {
  display: flex;
  justify-content: center;
  padding: 60rpx 0;
  color: #94a3b8;
  font-size: 26rpx;
}

/* 添加因素按钮 */
.add-factor-btn {
  margin-top: 28rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
  padding: 26rpx;
  border: 2rpx dashed #93c5fd;
  border-radius: 18rpx;
  background: #ffffff;
  transition: all 0.2s ease;
}

.add-factor-btn:active {
  transform: scale(0.98);
  background: #f8fafc;
  border-color: #60a5fa;
}

.add-factor-icon {
  font-size: 30rpx;
  color: #2563eb;
  font-weight: 300;
  line-height: 1;
}

.add-factor-text {
  font-size: 26rpx;
  color: #2563eb;
  font-weight: 600;
}

/* ========== 因素抽屉 ========== */
.drawer-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(15, 23, 42, 0.5);
  z-index: 2000;
  display: flex;
  align-items: flex-end;
  animation: maskIn 0.25s ease;
}

.drawer-popup {
  width: 100%;
  max-height: 88vh;
  background: #ffffff;
  border-radius: 36rpx 36rpx 0 0;
  display: flex;
  flex-direction: column;
  animation: popupIn 0.32s cubic-bezier(0.32, 0.72, 0, 1);
}

.drawer-header {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 32rpx;
  border-bottom: 1rpx solid #f1f5f9;
  position: relative;
  flex-shrink: 0;
}

.drawer-close {
  position: absolute;
  left: 32rpx;
  width: 52rpx;
  height: 52rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  background: #f1f5f9;
  transition: all 0.2s;
}

.drawer-close:active {
  background: #e2e8f0;
  transform: scale(0.92);
}

.drawer-title {
  font-size: 34rpx;
  font-weight: 700;
  color: #0f172a;
}

.drawer-scroll {
  flex: 1;
  min-height: 0;
  box-sizing: border-box;
}

.drawer-form {
  display: flex;
  flex-direction: column;
  gap: 26rpx;
  padding: 28rpx 32rpx;
}

.drawer-field {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

.drawer-label {
  font-size: 28rpx;
  color: #475569;
  font-weight: 600;
}

.drawer-label .required {
  color: #ef4444;
}

.drawer-input {
  width: 100%;
  height: 88rpx;
  background: #f8fafc;
  border-radius: 16rpx;
  padding: 0 24rpx;
  font-size: 28rpx;
  color: #1e293b;
  border: 2rpx solid #f1f5f9;
  box-sizing: border-box;
  transition: all 0.2s;
}

.drawer-input:focus {
  border-color: #2563eb;
  background: #ffffff;
  box-shadow: 0 0 0 4rpx rgba(37, 99, 235, 0.06);
}

.drawer-textarea {
  width: 100%;
  min-height: 140rpx;
  background: #f8fafc;
  border-radius: 16rpx;
  padding: 20rpx 24rpx;
  font-size: 28rpx;
  color: #1e293b;
  border: 2rpx solid #f1f5f9;
  box-sizing: border-box;
  transition: all 0.2s;
}

.drawer-textarea:focus {
  border-color: #2563eb;
  background: #ffffff;
  box-shadow: 0 0 0 4rpx rgba(37, 99, 235, 0.06);
}

.drawer-textarea-sm {
  min-height: 88rpx;
}

.drawer-textarea-lg {
  min-height: 220rpx;
}

.drawer-footer {
  display: flex;
  gap: 20rpx;
  padding: 20rpx 32rpx;
  padding-bottom: calc(24rpx + env(safe-area-inset-bottom));
  border-top: 1rpx solid #f1f5f9;
  flex-shrink: 0;
}

.drawer-cancel {
  flex: 1;
  height: 92rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #f1f5f9;
  border-radius: 46rpx;
  font-size: 30rpx;
  color: #64748b;
  font-weight: 600;
  transition: all 0.2s;
}

.drawer-cancel:active {
  background: #e2e8f0;
  transform: scale(0.97);
}

.drawer-submit {
  flex: 2;
  height: 92rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, #2563eb 0%, #6366f1 100%);
  border-radius: 46rpx;
  font-size: 30rpx;
  color: #fff;
  font-weight: 700;
  box-shadow: 0 8rpx 24rpx rgba(37, 99, 235, 0.3);
  transition: all 0.2s ease;
}

.drawer-submit:active {
  opacity: 0.9;
  transform: scale(0.97);
}

.drawer-submit.loading {
  opacity: 0.7;
  pointer-events: none;
}

/* ========== 因素插件市场入口 ========== */
.plugin-market-entry {
  display: flex;
  align-items: center;
  gap: 14rpx;
  padding: 24rpx 28rpx;
  background: linear-gradient(135deg, #f0f4ff 0%, #ede9fe 100%);
  border-radius: 18rpx;
  border: 2rpx solid #ddd6fe;
  transition: all 0.2s;
}

.plugin-market-entry:active {
  opacity: 0.85;
  transform: scale(0.98);
}

.plugin-market-icon {
  font-size: 38rpx;
  line-height: 1;
}

.plugin-market-label {
  flex: 1;
  font-size: 28rpx;
  font-weight: 700;
  color: #6d28d9;
}

.plugin-market-arrow {
  font-size: 36rpx;
  color: #a78bfa;
  font-weight: 700;
  line-height: 1;
}

/* ========== 新建/编辑智能体弹窗 ========== */
.create-popup {
  max-height: 72vh;
}

.create-form-scroll {
  flex: 1;
  min-height: 0;
  box-sizing: border-box;
}

.create-form {
  width: 100%;
  box-sizing: border-box;
  padding: 28rpx 32rpx;
  padding-bottom: calc(56rpx + env(safe-area-inset-bottom));
  display: flex;
  flex-direction: column;
  gap: 28rpx;
  overflow-x: hidden;
}

.create-form-item {
  display: flex;
  flex-direction: column;
  gap: 14rpx;
}

.create-form-label {
  display: flex;
  align-items: center;
  gap: 6rpx;
}

.create-label-text {
  font-size: 28rpx;
  font-weight: 700;
  color: #0f172a;
}

.create-label-required {
  font-size: 24rpx;
  color: #ef4444;
}

.create-input {
  width: 100%;
  height: 92rpx;
  background: #f8fafc;
  border-radius: 18rpx;
  padding: 0 28rpx;
  font-size: 28rpx;
  color: #1e293b;
  border: 2rpx solid #f1f5f9;
  box-sizing: border-box;
  transition: all 0.2s;
}

.create-input:focus {
  border-color: #2563eb;
  background: #ffffff;
  box-shadow: 0 0 0 4rpx rgba(37, 99, 235, 0.06);
}

.create-placeholder {
  color: #94a3b8;
}

.create-textarea {
  width: 100%;
  min-height: 170rpx;
  background: #f8fafc;
  border-radius: 18rpx;
  padding: 22rpx 28rpx;
  font-size: 28rpx;
  color: #1e293b;
  border: 2rpx solid #f1f5f9;
  box-sizing: border-box;
  transition: all 0.2s;
}

.create-textarea:focus {
  border-color: #2563eb;
  background: #ffffff;
  box-shadow: 0 0 0 4rpx rgba(37, 99, 235, 0.06);
}

.create-switch-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 28rpx;
  background: #f8fafc;
  border-radius: 18rpx;
  border: 2rpx solid #f1f5f9;
  transition: all 0.2s;
}

.create-switch-info {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
  flex: 1;
  min-width: 0;
}

.create-switch-desc {
  font-size: 22rpx;
  color: #94a3b8;
}

/* 自定义开关 */
.switch-wrapper {
  width: 92rpx;
  height: 54rpx;
  background: #cbd5e1;
  border-radius: 27rpx;
  position: relative;
  transition: background 0.25s ease;
  flex-shrink: 0;
}

.switch-wrapper.active {
  background: linear-gradient(135deg, #2563eb 0%, #6366f1 100%);
}

.switch-thumb {
  width: 46rpx;
  height: 46rpx;
  background: #ffffff;
  border-radius: 50%;
  position: absolute;
  top: 4rpx;
  left: 4rpx;
  transition: left 0.25s ease;
  box-shadow: 0 2rpx 6rpx rgba(0, 0, 0, 0.12);
}

.switch-wrapper.active .switch-thumb {
  left: 42rpx;
}

/* 提交按钮 */
.create-submit-btn {
  margin-top: 12rpx;
  width: 100%;
  height: 100rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, #2563eb 0%, #6366f1 100%);
  border-radius: 50rpx;
  color: #ffffff;
  font-size: 32rpx;
  font-weight: 700;
  box-shadow: 0 10rpx 28rpx rgba(37, 99, 235, 0.35);
  transition: all 0.2s ease;
}

.create-submit-btn:active {
  transform: scale(0.97);
  opacity: 0.9;
}

.create-submit-btn.loading {
  opacity: 0.7;
  pointer-events: none;
}

/* ========== 比赛选择弹窗 ========== */
.picker-content {
  width: 100%;
  box-sizing: border-box;
  padding: 24rpx 32rpx;
  padding-bottom: calc(56rpx + env(safe-area-inset-bottom));
  overflow-x: hidden;
}

.match-picker-loading {
  display: flex;
  justify-content: center;
  padding: 80rpx 0;
  color: #94a3b8;
  font-size: 28rpx;
}

.match-picker-list {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.match-picker-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 28rpx;
  background: #f8fafc;
  border-radius: 18rpx;
  border: 2rpx solid #f1f5f9;
  transition: all 0.2s ease;
}

.match-picker-item:active {
  transform: scale(0.98);
}

.match-picker-item.selected {
  border-color: #3b82f6;
  background: linear-gradient(135deg, #eff6ff 0%, #f0f9ff 100%);
}

.mp-match-info {
  display: flex;
  flex-direction: column;
  gap: 10rpx;
  flex: 1;
  min-width: 0;
}

.mp-match-header {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.mp-match-num {
  font-size: 22rpx;
  color: #2563eb;
  background: #dbeafe;
  padding: 4rpx 12rpx;
  border-radius: 6rpx;
  font-weight: 600;
}

.mp-match-league {
  font-size: 24rpx;
  color: #64748b;
  font-weight: 500;
}

.mp-match-teams {
  display: flex;
  align-items: center;
  gap: 14rpx;
}

.mp-team-name {
  font-size: 30rpx;
  font-weight: 700;
  color: #1e293b;
}

.mp-vs {
  font-size: 22rpx;
  color: #94a3b8;
  font-weight: 600;
}

.mp-match-time {
  font-size: 22rpx;
  color: #94a3b8;
}

.mp-check {
  width: 52rpx;
  height: 52rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, #2563eb 0%, #6366f1 100%);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  color: #fff;
  font-size: 28rpx;
  font-weight: 700;
  box-shadow: 0 4rpx 14rpx rgba(37, 99, 235, 0.35);
}

.match-picker-empty {
  display: flex;
  justify-content: center;
  padding: 80rpx 0;
  color: #94a3b8;
  font-size: 28rpx;
}

/* ========== 智能体选择弹窗 ========== */
.agent-picker-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(15, 23, 42, 0.4);
  z-index: 400;
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.3s ease;
}

.agent-picker-mask.visible {
  opacity: 1;
  pointer-events: auto;
}

.agent-picker-panel {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  max-height: 62vh;
  background: #fff;
  border-radius: 36rpx 36rpx 0 0;
  z-index: 401;
  transform: translateY(100%);
  transition: transform 0.35s cubic-bezier(0.32, 0.72, 0, 1);
  display: flex;
  flex-direction: column;
}

.agent-picker-panel.open {
  transform: translateY(0);
}

.agent-picker-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 32rpx 32rpx 16rpx;
  flex-shrink: 0;
}

.agent-picker-title {
  font-size: 34rpx;
  font-weight: 700;
  color: #0f172a;
}

.agent-picker-close {
  width: 56rpx;
  height: 56rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #f1f5f9;
  transition: all 0.2s;
}

.agent-picker-close:active {
  background: #e2e8f0;
  transform: scale(0.93);
}

.agent-picker-close-icon {
  font-size: 36rpx;
  color: #64748b;
  line-height: 1;
  font-weight: 300;
}

.agent-picker-body {
  flex: 1;
  min-height: 0;
  padding: 0 28rpx 40rpx;
}
